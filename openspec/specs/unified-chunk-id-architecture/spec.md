## Purpose

This specification addresses the chunk ID mismatch between the knowledge graph (Neo4j) and vector database (Milvus/OpenSearch). The knowledge graph stores `source_chunks` with IDs like `chunk_0`, `chunk_1` while the vector database stores chunks with UUID-based IDs like `0012df02-e7ab-4bb3-b81c-18f99dc541b2`. This mismatch causes KG-guided retrieval to fail because the ChunkResolver cannot find chunks by ID.

The solution ensures all three databases (PostgreSQL, Milvus/OpenSearch, Neo4j) use the same UUID-based chunk IDs generated at chunk creation time.


### Key Terms
- **Chunk_ID**: A unique identifier for a document chunk, must be a valid UUID (Python's uuid.uuid4())
- **Chunking_Framework**: The GenericMultiLevelChunkingFramework that processes documents into chunks
- **ProcessedChunk**: A dataclass representing a chunk with UUID-based ID, content, and metadata
- **Knowledge_Graph**: Neo4j database storing concepts and their source_chunks references
- **Vector_Database**: Milvus (local) or OpenSearch (AWS) storing chunk embeddings
- **Relational_Database**: PostgreSQL storing chunk content and metadata
- **ChunkResolver**: Component that resolves chunk IDs from Neo4j to actual content from vector store
- **Source_Chunks**: List of chunk IDs stored on ConceptNode in Neo4j, referencing chunks that mention the concept
- **Seeding_Script**: Scripts that populate databases with sample/test data

## Requirements

### Requirement: UUID Generation at Chunk Creation

The system SHALL support: As a system developer, I want chunk IDs to be generated as UUIDs at chunk creation time, so that all downstream systems use the same identifier.

#### Scenario: THE Chunking_Framework SHALL generate a valid UUID using Pyt

- **THEN** THE Chunking_Framework SHALL generate a valid UUID using Python's uuid.uuid4() for each ProcessedChunk at creation time

#### Scenario: THE uuid.uuid4() function SHALL use the operating system's c

- **THEN** THE uuid.uuid4() function SHALL use the operating system's cryptographically secure random number generator (os.urandom), which is stateless and unaffected by system restarts

#### Scenario: WHEN a ProcessedChunk is created with an invalid ID THEN THE

- **WHEN** a ProcessedChunk is created with an invalid ID
- **THEN** THE ProcessedChunk SHALL raise a ValueError with the invalid ID in the message

#### Scenario: THE ProcessedChunk SHALL validate that its ID is a valid UUI

- **THEN** THE ProcessedChunk SHALL validate that its ID is a valid UUID in the __post_init__ method

#### Scenario: WHEN a chunk is split into sub-chunks THEN THE Chunking_Fram

- **WHEN** a chunk is split into sub-chunks
- **THEN** THE Chunking_Framework SHALL generate new UUIDs for each sub-chunk

#### Scenario: THE system SHALL NOT maintain any state for UUID generation,

- **THEN** THE system SHALL NOT maintain any state for UUID generation, ensuring restarts cannot cause ID conflicts

### Requirement: UUID Propagation to PostgreSQL

The system SHALL support: As a system developer, I want PostgreSQL to store chunks with the same UUID generated by the chunking framework, so that chunk IDs are consistent.

#### Scenario: WHEN storing chunks in PostgreSQL THEN THE Storage_Service S

- **WHEN** storing chunks in PostgreSQL
- **THEN** THE Storage_Service SHALL use the chunk's existing UUID unchanged

#### Scenario: WHEN a chunk with an invalid UUID is passed to PostgreSQL st

- **WHEN** a chunk with an invalid UUID is passed to PostgreSQL storage
- **THEN** THE Storage_Service SHALL raise a ValueError

#### Scenario: THE document_chunks table SHALL use the chunk UUID as the pr

- **THEN** THE document_chunks table SHALL use the chunk UUID as the primary key

#### Scenario: WHEN a document is reprocessed THEN THE Storage_Service SHAL

- **WHEN** a document is reprocessed
- **THEN** THE Storage_Service SHALL delete old chunks and insert new ones with new UUIDs

### Requirement: UUID Propagation to Vector Database

The system SHALL support: As a system developer, I want the vector database to store chunks with the same UUID generated by the chunking framework, so that chunk IDs are consistent.

#### Scenario: WHEN storing embeddings in Milvus/OpenSearch THEN THE Vector

- **WHEN** storing embeddings in Milvus/OpenSearch
- **THEN** THE Vector_Client SHALL use the chunk's existing UUID unchanged

#### Scenario: WHEN a chunk with an invalid UUID is passed to vector storag

- **WHEN** a chunk with an invalid UUID is passed to vector storage
- **THEN** THE Vector_Client SHALL raise a ValueError with the invalid ID in the message

#### Scenario: THE Vector_Client SHALL NOT generate new IDs for chunks

- **THEN** THE Vector_Client SHALL NOT generate new IDs for chunks

#### Scenario: WHEN a document is reprocessed THEN THE Vector_Client SHALL

- **WHEN** a document is reprocessed
- **THEN** THE Vector_Client SHALL delete old chunks before storing new ones

### Requirement: UUID Propagation to Knowledge Graph

The system SHALL support: As a system developer, I want the knowledge graph to store source_chunks with the same UUIDs used in PostgreSQL and Milvus, so that ChunkResolver can find chunks.

#### Scenario: WHEN creating ConceptNode in Neo4j THEN THE KG_Builder SHALL

- **WHEN** creating ConceptNode in Neo4j
- **THEN** THE KG_Builder SHALL use the chunk's existing UUID in source_chunks

#### Scenario: WHEN creating relationships in Neo4j THEN THE KG_Builder SHA

- **WHEN** creating relationships in Neo4j
- **THEN** THE KG_Builder SHALL use chunk UUIDs in evidence_chunks

#### Scenario: THE source_chunks field on ConceptNode SHALL contain only va

- **THEN** THE source_chunks field on ConceptNode SHALL contain only valid UUIDs

#### Scenario: WHEN a document is reprocessed THEN THE KG_Service SHALL upd

- **WHEN** a document is reprocessed
- **THEN** THE KG_Service SHALL update source_chunks references to use new UUIDs

### Requirement: Chunk Resolution Across Databases

The system SHALL support: As a system developer, I want the ChunkResolver to successfully resolve chunk IDs from Neo4j to content in the vector database, so that KG-guided retrieval works.

#### Scenario: WHEN ChunkResolver receives chunk IDs from Neo4j THEN THE Ch

- **WHEN** ChunkResolver receives chunk IDs from Neo4j
- **THEN** THE ChunkResolver SHALL find matching chunks in the vector database

#### Scenario: WHEN a chunk ID from source_chunks does not exist in the vec

- **WHEN** a chunk ID from source_chunks does not exist in the vector database
- **THEN** THE ChunkResolver SHALL log a warning and continue with remaining chunks

#### Scenario: THE ChunkResolver SHALL return RetrievedChunk objects with c

- **THEN** THE ChunkResolver SHALL return RetrievedChunk objects with content populated for all found chunks

#### Scenario: FOR ALL chunks stored via the document processing pipeline,

- **GIVEN** ALL chunks stored via the document processing pipeline, the chunk ID
- **THEN** FOR ALL chunks stored via the document processing pipeline, the chunk ID SHALL be resolvable by ChunkResolver

### Requirement: Database Purge for Clean State

The system SHALL support: As a system administrator, I want to purge all existing chunk data from all databases, so that the system starts fresh with consistent UUID-based chunk IDs.

#### Scenario: THE Purge_Script SHALL delete all chunk data from PostgreSQL

- **THEN** THE Purge_Script SHALL delete all chunk data from PostgreSQL (document_chunks table)

#### Scenario: THE Purge_Script SHALL delete all chunk embeddings from Milv

- **THEN** THE Purge_Script SHALL delete all chunk embeddings from Milvus/OpenSearch

#### Scenario: THE Purge_Script SHALL delete all Chunk nodes and source_chu

- **THEN** THE Purge_Script SHALL delete all Chunk nodes and source_chunks references from Neo4j

#### Scenario: THE Purge_Script SHALL delete all Concept nodes that referen

- **THEN** THE Purge_Script SHALL delete all Concept nodes that reference invalid chunk IDs

#### Scenario: THE Purge_Script SHALL provide a summary of deleted records

- **THEN** THE Purge_Script SHALL provide a summary of deleted records from each database

#### Scenario: AFTER purge completes THEN THE system SHALL be ready for fre

- **THEN** AFTER purge completes THEN THE system SHALL be ready for fresh document uploads with consistent UUIDs

### Requirement: Seeding Script Compliance

The system SHALL support: As a developer, I want all seeding scripts to use UUID format for chunk IDs, so that test data is consistent with production data.

#### Scenario: THE Neo4j sample data script SHALL use valid UUIDs for all c

- **THEN** THE Neo4j sample data script SHALL use valid UUIDs for all chunk IDs

#### Scenario: THE document-concept association script SHALL use valid UUID

- **THEN** THE document-concept association script SHALL use valid UUIDs when creating chunk nodes

#### Scenario: WHEN a seeding script creates a chunk THEN THE script SHALL

- **WHEN** a seeding script creates a chunk
- **THEN** THE script SHALL generate a UUID using uuid.uuid4()

#### Scenario: THE seeding scripts SHALL NOT use formats like "chunk_0", "c

- **THEN** THE seeding scripts SHALL NOT use formats like "chunk_0", "chunk_001", or "doc_chunk_1"

### Requirement: Validation and Error Handling

The system SHALL support: As a system developer, I want clear validation and error messages when invalid chunk IDs are used, so that issues are caught early.

#### Scenario: WHEN an invalid chunk ID is detected THEN THE system SHALL r

- **WHEN** an invalid chunk ID is detected
- **THEN** THE system SHALL raise a ValueError with the invalid ID in the message

#### Scenario: THE validation SHALL occur at chunk creation time in Process

- **THEN** THE validation SHALL occur at chunk creation time in ProcessedChunk

#### Scenario: THE validation SHALL occur at storage time in PostgreSQL, Mi

- **THEN** THE validation SHALL occur at storage time in PostgreSQL, Milvus, and Neo4j storage functions

#### Scenario: IF a chunk ID validation fails THEN THE error message SHALL

- **GIVEN** a chunk ID validation fails
- **THEN** IF a chunk ID validation fails THEN THE error message SHALL include the invalid ID value

#### Scenario: THE system SHALL fail fast on invalid chunk IDs rather than

- **THEN** THE system SHALL fail fast on invalid chunk IDs rather than silently generating new ones
