use_default_settings:
  engines:
    keep_only:
      - google
      - bing
      - brave
      - duckduckgo
      - wikipedia
      - wikidata
      - google scholar
      - arxiv
      - github
      - stackoverflow

server:
  secret_key: "dev-searxng-secret"
  bind_address: "0.0.0.0"
  port: 8080
search:
  formats:
    - html
    - json
