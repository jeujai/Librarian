# Minimal enhanced requirements for cost-optimized deployment
# Only the absolute essentials for the web interface

# Core FastAPI and web framework dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
gunicorn==21.2.0
python-multipart==0.0.6

# Security dependencies (minimal versions)
cryptography>=41.0.0
PyJWT>=2.8.0

# Database dependencies
psycopg2-binary==2.9.9

# Environment and configuration
python-dotenv==1.0.0
pydantic==2.5.0
pydantic-settings==2.1.0

# AWS dependencies
boto3==1.34.0
botocore==1.34.0

# Basic utilities
requests==2.31.0